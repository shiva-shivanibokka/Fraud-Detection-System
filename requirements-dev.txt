# Dev / CI dependencies: the runtime API deps plus test + lint tooling.
# Install with:  pip install -r requirements-dev.txt
-r requirements-api.txt

pytest==9.1.1
httpx==0.28.1
ruff==0.15.12
# Offline velocity engine — exercised by tests/test_velocity_duckdb.py
duckdb==1.5.4
